import { MessengerNotification } from './messenger-notification';

describe('MessengerNotification', () => {
  it('should create an instance', () => {
    expect(new MessengerNotification()).toBeTruthy();
  });
});
